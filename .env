# Shared across every mode.
# VITE_API_URL is intentionally NOT set here — it lives in .env.development
# (local backend) and .env.production (deployed API), so `npm run dev` never
# talks to production. Mode files override this one.
VITE_GOOGLE_CLIENT_ID=dufdfudfud
